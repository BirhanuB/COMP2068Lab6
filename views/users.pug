extends layout
block content
    if (users.length)
        table
            tr
                th= "User ID"
                th= "|"
                th= "User Name"
            each registeredUser in users             
                tr
                    td= registeredUser._id
                    td= "|"
                    td= registeredUser.username
    else
        h1 No users found